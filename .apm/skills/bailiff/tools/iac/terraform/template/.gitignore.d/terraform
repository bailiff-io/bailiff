# Terraform / OpenTofu -- local working dirs and state. The lock file is committed.
.terraform/
*.tfstate
*.tfstate.backup
.terraform.tfstate.lock.info

# Override files (local customisation — never commit)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config files
.terraformrc
terraform.rc
