{#-
  terraform.tfvars.example -- copy to terraform.tfvars and fill in real values.
  Add terraform.tfvars to .gitignore when it holds anything sensitive.
-#}
# terraform.tfvars.example — copy to terraform.tfvars and set real values.
# Do NOT commit terraform.tfvars if it contains secrets.

# environment = "dev"
