{#-
  main.tf -- starter file. Provider configuration and resource definitions go
  here. No workspaces are scaffolded; a directory per environment is the pattern
  this layout assumes.
-#}
# main.tf — add provider config and resource definitions here.
#
# Example provider configuration:
# provider "aws" {
#   region = var.aws_region
# }
