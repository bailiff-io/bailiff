import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
{% if test_runner == 'vitest-node' %}
    environment: "node",
{% elif test_runner == 'vitest-browser' %}
    browser: {
      enabled: true,
      provider: "playwright",
      instances: [{ browser: "chromium" }],
    },
{% elif test_runner == 'vitest+playwright' %}
    environment: "node",
    // playwright e2e tests run separately via `playwright test`
{% endif %}
    include: ["src/**/*.{test,spec}.{ts,tsx}"],
    coverage: {
      provider: "v8",
      // lcov is what codecov and coveralls read; text is what a developer reads.
      reporter: ["text", "lcov"],
{%- if ts_coverage is defined and ts_coverage and ts_coverage_min and ts_coverage_min > 0 %}
      thresholds: {
        lines: {{ ts_coverage_min }},
      },
{%- endif %}
    },
  },
});
