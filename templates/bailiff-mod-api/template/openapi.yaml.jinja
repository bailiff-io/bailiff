# openapi.yaml — seeded by bailiff-mod-api; this file is yours to edit.
# Re-runs never overwrite it (_skip_if_exists).
openapi: 3.1.0
info:
  title: "{{ project_name | default('API', true) }}"
  description: "{{ description }}"
  version: 0.1.0
paths: {}
components:
  schemas: {}
